import { OrdertotalPipe } from './ordertotal.pipe';

describe('OrdertotalPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdertotalPipe();
    expect(pipe).toBeTruthy();
  });
});
